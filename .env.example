# === Backend ===
# Segredo para assinar tokens JWT (obrigatório em produção)
JWT_SECRET=troque-para-um-segredo-forte

# === GLPI (opcional - pode configurar pelo painel na interface) ===
# URL base do servidor GLPI (usado como proxy em desenvolvimento para evitar CORS)
VITE_GLPI_URL=https://seu-servidor-glpi.com

# Credenciais da API GLPI
# App Token: Configuração > Geral > API > Token de aplicação cliente
VITE_GLPI_APP_TOKEN=
# User Token: Minhas configurações > Controle remoto > Token de acesso pessoal
VITE_GLPI_USER_TOKEN=

# === WhatsApp (WhAPI.cloud) ===
# Token de autenticação da API Whapi.cloud (painel: https://panel.whapi.cloud)
WHAPI_TOKEN=
